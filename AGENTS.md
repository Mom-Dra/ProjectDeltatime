# ProjectDeltatime 작업 규칙

## 프로젝트 경로

- 저장소 루트는 이 파일이 있는 디렉터리다.
- 실제 Unity 프로젝트 루트는 `ProjectDeltatime/`이다.
- 기획 문서는 저장소 루트의 `Docs/PROJECT_DESIGN_DOCUMENT.md`, 기능 변경 기록은 `Docs/FEATURE_CHANGELOG.md`다.

## 필수 문서 갱신 규칙

- Unity 프로젝트의 기능을 추가, 수정, 삭제할 때 관련 기획서도 함께 검토한다.
- 사용자가 명시적으로 문서 갱신을 제외하지 않는 한, 기능 구현 작업의 마지막 단계에서 문서를 갱신한다.
- 기능 변경이 기획서에 영향을 주면 `Docs/PROJECT_DESIGN_DOCUMENT.md`를 갱신한다.
- 모든 기능 변경은 `Docs/FEATURE_CHANGELOG.md`에 기록한다.
- 문서에는 기능의 구현 상태를 `구현 완료`, `부분 구현`, `미구현`, `계획 필요`, `확인 불가` 중 하나로 반드시 표시한다.
- 코드나 에셋에서 확인할 수 없는 내용은 사실처럼 작성하지 않는다. 불가피한 해석은 `추정`이라고 명시한다.
- 주요 기능 설명에는 근거가 되는 저장소 기준 파일 경로를 기록한다.
- 밸런스 값은 코드 기본값보다 씬, 프리팹, ScriptableObject에 직렬화된 실제 값을 우선 확인한다.
- 기존 문서 내용을 삭제하기 전에 현재 구현 상태와 충돌하는지 확인한다. 충돌하지 않는 기존 의사결정과 이력은 보존한다.
- 문서 갱신만을 위해 코드나 에셋을 임의로 수정하지 않는다.

## 작업 완료 전 확인

- 모든 작업이 끝나기 전에 `git status`와 `git diff`로 변경된 파일과 기존 사용자 변경을 확인한다.
- 기존 작업 트리가 변경된 상태라면 사용자 변경을 보존하고 이번 작업의 변경과 구분한다.
- 기능에 관련된 테스트를 실행하거나 기존 결과를 확인한다. 실행하지 못한 테스트는 문서와 변경 기록에 `미실행` 또는 `확인 불가`로 남긴다.
- 과거 테스트 로그를 최신 변경의 결과처럼 기록하지 않는다. 구현 파일의 변경 시각 또는 커밋이 테스트보다 최신이면 재검증 대상으로 표시한다.
- 문서 수정 후 최종 보고에 수정한 문서 경로, 반영한 기능, 테스트 결과, 남은 미구현/확인 항목을 요약한다.

## Unity 프로젝트 주의사항

- `ProjectDeltatime/Assets/_Project/Scripts/Editor/PrototypeSceneBuilder.cs`는 씬, 프리팹, 머티리얼, 무기 데이터와 빌드 설정을 재생성할 수 있다. 실행 전에 수동 변경과 충돌하는지 확인한다.
- 월드 시간의 영향을 받아야 하는 신규 시뮬레이션은 `WorldTimeController.WorldDeltaTime` 사용 여부를 검토한다. 전역 `Time.timeScale`을 임의로 변경하지 않는다.
- 새 시야 장애물은 현재 Layer 8 `VisionObstacle` 정책과 충돌하지 않는지 확인한다.
- 입력 변경 시 `PlayerControls.inputactions`, 생성 래퍼, HUD 조작 안내, 스모크 검증을 함께 확인한다.
- 씬, 프리팹, ScriptableObject 변경에는 해당 `.meta`와 참조 GUID 상태를 함께 확인한다.

## 기능 변경 기록 형식

`Docs/FEATURE_CHANGELOG.md`에 다음 항목을 작성한다.

- 날짜와 기능명
- 변경 유형
- 변경 내용
- 영향을 받은 시스템
- 관련 파일
- 기획서 반영 내용
- 테스트 결과
- 남은 작업
